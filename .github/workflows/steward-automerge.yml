on:
  push:
    branches:
    - 'update/*'
  check_suite:
    types:
    - completed

name: Steward automerge

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: merge PR
      uses: pascalgn/automerge-action@v0.16.2
      with:
        GITHUB_TOKEN: ${{ secrets.COMMIT_TOKEN }}
        MERGE_LABELS: ""
        MERGE_DELETE_BRANCH: true
        ALLOWED_UPDATE: minor
        MERGE_METHOD: squash
